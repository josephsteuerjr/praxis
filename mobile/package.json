{
  "name": "frame-mobile",
  "private": true,
  "version": "0.4.2",
  "type": "module",
  "scripts": {
    "dev": "vite --port 5175 --strictPort --host",
    "build": "tsc --noEmit && vite build"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^8.2.2"
  }
}
