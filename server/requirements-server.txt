# Сверх requirements.txt поставки — только серверное.
# STT: та же коробка, что у Праксис (faster-whisper, модель large-v3-turbo
# int8 в томе /models). На Windows этого нет сознательно: там голосовые
# агент не слышит, и текст об этом честный.
faster-whisper==1.2.1
